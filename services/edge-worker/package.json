{
  "name": "echoweave-edge-worker",
  "version": "0.1.3",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute ECHOWEAVE_DB --file=./schema.sql",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8",
    "wrangler": "^4.8.0"
  }
}
